function FristComponent(){   // we can pass value as property
    let city = "pune";
    return (
        <div>
            <p>we live in {pune}</p>
                            
        </div>
    )
}

export default FristComponent;

//we cant use object directly in {} dirctly but we can use array

// to add class we use "className" atribute.